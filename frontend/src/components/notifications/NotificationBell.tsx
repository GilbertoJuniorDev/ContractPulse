/**
 * Ícone de sino com badge de notificações não lidas.
 * Usa Supabase Realtime para atualização em tempo real.
 */
export default function NotificationBell() {
  return <div>{/* A ser implementado */}</div>;
}
