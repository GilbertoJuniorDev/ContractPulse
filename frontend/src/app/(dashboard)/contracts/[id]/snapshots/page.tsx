export default function SnapshotsPage({
  params,
}: {
  params: { id: string };
}) {
  return (
    <div>
      <h1>Evidências</h1>
      {/* Snapshots para Profit Sharing — a ser implementado */}
    </div>
  );
}
