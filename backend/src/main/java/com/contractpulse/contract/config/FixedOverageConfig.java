package com.contractpulse.contract.config;

/**
 * POJO de configuração para contratos do tipo FIXED_OVERAGE.
 * Serializado como JSONB no campo config do contrato.
 */
public class FixedOverageConfig {
    // A ser implementado
}
