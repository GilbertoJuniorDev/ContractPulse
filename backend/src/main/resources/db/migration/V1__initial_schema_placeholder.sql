-- ContractPulse — Migration placeholder
-- As tabelas serão criadas nas migrations subsequentes conforme o desenvolvimento avança.
-- Este arquivo garante que o Flyway está configurado corretamente.

SELECT 1;
